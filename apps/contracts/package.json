{
  "name": "contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Smart contracts for the Blockchain-Based Budget Allocation and Expense Monitoring System",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "smoke": "hardhat run scripts/smoke.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  }
}
