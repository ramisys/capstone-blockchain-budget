/**
 * Currency configuration used for formatting monetary values across the app.
 * Adjust `CURRENCY_CODE` to change the locale used by `Intl.NumberFormat`.
 */
export const CURRENCY = {
  CODE: 'PHP',
  LOCALE: 'en-PH',
};
